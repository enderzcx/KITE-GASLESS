# Judge Walkthrough (90s)

## 0-20s
- Open `https://kiteclaw.duckdns.org/`.
- Click `Run Demo`.
- Explain: each paid call is `ERC8004 identity -> x402 challenge -> payment -> proof -> on-chain confirmation`.

## 20-45s
- Point to chart update and evidence panel.
- Show `requestId`, `txHash`, `block`, `status`, `explorer` in same flow.
- Explain: chart point appears only after paid unlock.

## 45-70s
- Open `https://kiteclaw.duckdns.org/market`.
- Select `BTC Risk Score (A2A)`.
- Click `Invoke Service`.
- Show service receipt updates with paid status.

## 70-90s
- Open `https://kiteclaw.duckdns.org/ops`.
- Show trace list + live feed + evidence drawer.
- Close with thesis: micropayments are the bridge layer for verifiable agent cooperation.
